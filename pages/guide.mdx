import Link from "next/link";
import { Card } from "@/components";
import { SquarePen, BookMarked } from "lucide-react";

# Guide

Welcome to hookform-field! This is a plugin manage fields for react-hook-form.
You can use it as a starting point for your own project.

## Exploring

To explore how to install hookform-field and learn about its API, please follow these:

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6">
  <Link href="/quick-start">
    <Card
      icon={<SquarePen />}
      label="Create Field"
      description="Create a root field that contains all inputs in your project"
    />
  </Link>

  <Link href="/api-reference">
    <Card
      icon={<BookMarked />}
      label="Using <Form />"
      description="Listen submit data instead FormProvider & useForm"
    />
  </Link>
</div>
